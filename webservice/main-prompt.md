나는 1인 풀스택 개발자다. 6개월 내 첫 $100 매출이 가능한 웹서비스 아이디어를 찾고 있다.
Sub-agents (market-researcher, red-team, scorer)를 활용해서 TOP 3을 선정해줘.
추상어 사용 금지. 모든 판단에 출처와 숫자 동반. whereirank.net 교훈 적용: 데이터 부재 발견 시 즉시 KILL.

# Phase 0: 입력 확인

다음 3가지를 사용자에게 질문하고, **사용자 응답을 받기 전까지 Phase 1 진행 금지.**

1. **타겟 유형**: B2B / B2C / 둘 다 OK [기본: 둘 다 OK]
2. **우선 지역**: 글로벌 / 한국 / 일본 / 동남아 / 미국 / 기타 [기본: 글로벌]
3. **이전 시도 및 실패** (선택):
   - whereirank.net (2026-05-11 STOP) — 직무별 연차별 연봉 비교. 핵심 데이터(직종×연차별 분위수) 공공통계에 부재. 교훈: AI가 "할 수 있다"고 해도 데이터를 직접 호출해 확인 후 코딩.

# Phase 1: 페인 포인트 수집 (market-researcher sub-agent)

**market-researcher sub-agent를 호출**해서 페인 포인트를 수집해줘.
입력 전달: 타겟 유형 + 우선 지역 + 아래 검색 지시.

## sub-agent 검색 지시 (반드시 전달)

- **검색 소스**: Reddit (r/SaaS, r/startups, r/webdev, r/smallbusiness), IndieHackers, Product Hunt 최근 30일, Hacker News "Ask HN"
- 각 페인 포인트에 **출처 URL 필수**. 출처 URL 없는 항목은 카운트하지 않음.
- **수량 조건**: 출처 URL이 있는 페인 포인트 10개 이상이면 OK. 수량 채우기용 일반론("시간이 부족하다", "이메일 관리가 힘들다" 등) 금지.
- 각 페인 포인트 형식: `# | 페인 포인트 요약(20자 이내) | 누가 겪는가 | 출처 URL | 언급 빈도(게시글/댓글 수)`

sub-agent 결과를 받은 후 Phase 2로 진행. 출처 URL이 있는 항목이 10개 미만이면 검색 키워드를 변경해서 재호출.

# Phase 2: 아이디어 생성 (메인이 직접)

수집된 페인 포인트 각각에 대해 1~2개 솔루션 아이디어를 생성한다 (15~20개).

각 아이디어 형식:
- **이름** (임시, 5~15자)
- **한 줄 설명** (50자 이내)
- **타겟 사용자** (구체적 직군/상황)
- **연결된 페인 포인트 #** (Phase 1 표 번호)
- **수익 모델**: 누가 / 왜 / 얼마(USD/월 또는 일회성)
- **핵심 외부 데이터/API**: URL 또는 명칭

# Phase 3: 킬 체크리스트 (메인이 직접, 정량 6항목)

각 아이디어를 ✅/❌/⚠️로 채점. ❌가 하나라도 있으면 즉시 탈락.

1. **데이터/API docs 검증**:
   - API 문서 URL 명시
   - 무료 티어 또는 freemium 존재 확인
   - docs의 sample response가 MVP에 필요한 필드를 포함하는지 확인
   - 상업적 사용 약관 확인
   - 판정: ✅ (docs에서 필드 매칭 확인) / ⚠️ (docs 불충분, Phase 6-2에서 실호출 필수) / ❌ (API 자체가 없거나 상업 사용 불가)
   - **⚠️인 경우 반드시 표시: "docs 기반 확인만 완료. 실호출 미검증 — whereirank.net 리스크"**
2. **경쟁 조사**: 경쟁자 0~1개면 이유 명시 / 2개+면 "X는 Y를 안 한다" 형식
3. **수익 모델 정량성**: USD 가격 명시 (B2B $20~$200/월 or B2C $3~$30/월 범위), 첫 $100까지 필요한 사용자 수 계산
4. **1인 운영 부담**: 월 5h 초과 유지보수 / 24/7 응답 / 월 $100 초과 운영비 → ❌
5. **1인 안티패턴**: 해당 시 단계별 대응 방안 (1주차 X, 2주차 Y 형식)
6. **규제·법적 리스크**: 의료/금융/법률 자문 → ❌

## Phase 3 전멸 대응

생존 아이디어가 3개 미만이면:
- Phase 1로 돌아가서 **검색 키워드를 변경**해서 재수집 (최대 1회)
- 재시도 후에도 3개 미만이면 생존한 것만으로 Phase 4 진행하고, 부족분 명시

# Phase 4: Red Team 비판 (red-team sub-agent)

킬 체크 통과 아이디어를 **red-team sub-agent에 전달**해서 각각 5가지 kill 사유 발굴.

## 교차 검증 절차 (자기심판 방지)

1. red-team sub-agent가 아이디어당 kill 사유 5개 제출
2. 메인이 각 kill 사유에 대해 **반박문** 작성 (근거 + 출처 필수)
3. 반박문을 다시 **red-team sub-agent에 전달**해서 "반박 유효 / 반박 무효" 2차 판정
4. 2차 판정에서 **반박 무효 3개 이상** → 해당 아이디어 탈락
5. 모든 kill 사유와 반박 과정을 **사용자에게 그대로 노출** (숨기지 않음)

## Phase 4 전멸 대응

생존 아이디어가 0개이면:
- Phase 1로 돌아가서 검색 키워드를 변경해서 재수집 (최대 1회)
- 재시도 후에도 0개이면 "생존 아이디어 없음" 보고 후 종료

# Phase 5: 점수화 (scorer sub-agent)

Red Team 통과 아이디어를 **scorer sub-agent에 전달**해서 아래 rubric으로 100점 만점 채점.

## 채점 rubric (반드시 sub-agent에 전달)

| 항목 | 배점 | ✅ 기준 |
|------|------|---------|
| 시장 크기 근거 | 20 | TAM 숫자 + 출처 URL 제시 |
| 수익까지 속도 | 20 | 첫 $100까지 예상 주 수 (4주 이내=20, 8주=15, 12주=10, 12주+=5) |
| 기술 난이도 | 20 | 1인 50h 빌드 가능=20, 100h=10, 100h+=5 |
| 경쟁 차별점 | 20 | 기존 경쟁자 대비 구체적 차이 1개+=20, 차이 불분명=5 |
| 데이터 확보 확실성 | 20 | Phase 3에서 ✅=20, ⚠️=10, ❌면 여기 도달 불가 |

점수 분산 점검: 모든 점수가 5점 이내로 모이면 항목별 점수 근거를 재작성하도록 재호출 (채점 회피 의심).

# Phase 6: TOP 3 + MVP 명세 (메인이 직접)

점수 70+ 우선, 50~69 보충해서 TOP 3 선정. 3개 미만이면 있는 만큼만 선정하고 부족분 명시.
각 아이디어마다:

## 6-1. MVP 명세 (50시간 빌드 가능)
- 기능 리스트 (3~7개, 각 빌드 시간 추정)
- 사용 기술 스택 (Cloudflare Workers/Pages/D1, AI API, 무료/저비용 기타)
- 예상 총 빌드 시간
- 운영비 추정 (월 USD)

## 6-2. 48시간 사전 검증 플랜 (코딩 시작 전 필수, 최소 2개 통과)

1. **데이터/API 실호출 테스트**: 환경변수 발급 후 직접 호출, 응답 충분 확인 (whereirank.net 교훈). Phase 3에서 ⚠️였던 항목은 여기서 반드시 검증.
2. **랜딩 페이지 + Stripe 결제 의향 측정**: 24h 노출, 가입 5+ 또는 결제 클릭 1+
3. **타겟 사용자 3~5명 직접 인터뷰**: "$X 낼 의향" 답변 50%+

## 6-3. 첫 $100 매출 로드맵 (주 단위)

## 6-4. 최대 리스크 1개 + 완화 방법 (단계별 행동)

# 출력 규칙

- 각 Phase 완료 시 한 줄 요약 출력 (`Phase X 완료: Y건 생성, Z건 탈락`)
- Sub-agent 호출 시 명시적으로 "Use market-researcher subagent" / "Use red-team subagent" / "Use scorer subagent" 사용
- 모든 추상어 금지: "명확한", "구체적", "잠재력", "흥미로운", "잘 만든다" 등
- 모든 판단에 출처(URL) + 숫자 동반
- Sub-agent 응답이 의심스러우면 재호출
- 파일 출력은 ./output/{YYYY-MM-DD}_{HHmm}_{타겟유형}_{우선지역}.md 형식
- **모든 출력은 한국어로 작성. Sub-agent 호출 시에도 한국어 결과 요구. 영문 결과는 한국어로 번역 후 출력.**
